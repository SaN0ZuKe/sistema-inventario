<div class="container">
  <div class="row">
     <div class="col-sm-4">
     </div>

     <div class="col-sm-4">
       </br></br></br>
        <h2>Inicio de sesión</h2></br>
         <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
           <div class="form-group">
             <%= f.input :email, label: "Correo Electrónico", required: false, autofocus: true, class: 'form-control' %>
             <%= f.input :password, label: "Contraseña", required: false, class: 'form-control'  %>
           </div>
           <div class="form-group">
             <%= f.button :submit, "Iniciar sesión", class: "btn btn-primary" %>
           </div>
         <% end %>
       <%= render "devise/shared/links" %>
     </div>

     <div class="col-sm-4"></div>
  </div>
</div>
